package com.school.attendance.config;

// Database configuration moved to main application class to avoid conflicts
public class DatabaseConfig {
    // JPA configuration now handled by main application class
}
